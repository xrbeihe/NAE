# ANE — AI Narrative Engine
# Phase 1 MVP Dependencies

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0

# Validation
pydantic>=2.7.0

# File uploads (worldview packs)
python-multipart>=0.0.9

# Async HTTP (for LLM API calls)
httpx>=0.27.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx  # also used as TestClient dependency via FastAPI

# Content parsing (NSFW scraper)
readability-lxml
